import React from 'react';
import styles from './PagesBar.module.css';

function PagesBar({ totalPages, currentPage }) {
    return (
        <div className={styles.pagesList}>
            
        </div>
    );
}

export default PagesBar;